/**
 * Archivo que se encarga de crear el contexto para la autentificación del usuario
 */

import { createContext } from "react";

const AuthContext = createContext(null);

export default AuthContext;
